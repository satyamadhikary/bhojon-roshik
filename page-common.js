(function(){var body=document.body,header=document.getElementById('siteHeader'),menuToggle=document.getElementById('menuToggle'),drawer=document.getElementById('mobileDrawer');function setHeader(){if(header)header.classList.toggle('scrolled',window.scrollY>10)}function setDrawer(open){if(!drawer||!menuToggle)return;drawer.classList.toggle('open',open);drawer.setAttribute('aria-hidden',String(!open));menuToggle.setAttribute('aria-expanded',String(open));body.classList.toggle('drawer-open',open)}function setActiveNav(){var path=(location.pathname.split('/').pop()||'get-started.html').toLowerCase();document.querySelectorAll('.desktop-nav a, .drawer-nav a').forEach(function(link){var href=(link.getAttribute('href')||'').toLowerCase();if(!href)return;link.classList.toggle('home-link',href===path)})}if(menuToggle&&drawer){menuToggle.addEventListener('click',function(){setDrawer(!drawer.classList.contains('open'))});drawer.querySelectorAll('[data-close-drawer], .drawer-link').forEach(function(n){n.addEventListener('click',function(){setDrawer(false)})});document.addEventListener('keydown',function(e){if(e.key==='Escape'&&drawer.classList.contains('open'))setDrawer(false)})}setHeader();setActiveNav();window.addEventListener('scroll',setHeader,{passive:true});})();
